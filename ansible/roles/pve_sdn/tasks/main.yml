---
# Placeholder. Cluster/Ceph/SDN bootstrap is deliberately NOT fully automated:
# these operations are one-shot and destructive if repeated incorrectly.
# See docs/06-phase3-cluster-build.md and docs/07-phase3-ceph-and-sdn.md for the
# guarded procedures. Use this role for post-bootstrap CONVERGENCE only
# (config file management, tuning values, module enablement).
- name: Placeholder
  ansible.builtin.debug:
    msg: "Role pve_sdn: add convergence tasks here, not bootstrap tasks."
